---
import '../styles/global.css';
import { Font } from 'astro:assets';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';

interface Props { title: string; description: string; ogImage?: string; }
const { title, description, ogImage = '/og/default.png' } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site ?? 'https://chaosofzen.com');
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical.href} />
    {/* The site declared no icon at all until now, so every browser fell back to
        /favicon.ico -- which was still the Astro starter icon. Safari only gained
        SVG favicon support in version 26, so the raster fallbacks are not
        belt-and-braces; they are what most Safari installs will actually use. */}
    <link rel="icon" href="/favicon.ico" sizes="48x48" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/icon-32.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(ogImage, canonical).href} />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image" />
    {/* Preload only the two faces that paint first -- body regular and the
        display weight used by every heading. The other weights, and the italic
        the manual needs, are fetched lazily by the browser only where a glyph
        actually uses them. */}
    <Font cssVariable="--font-body" preload={[{ weight: 400, style: 'normal' }]} />
    <Font cssVariable="--font-display" preload={[{ weight: 300, style: 'normal' }]} />
    <Font cssVariable="--font-code" />
  </head>
  <body class="bg-canvas text-ink antialiased">
    <a href="#main" class="sr-only focus:not-sr-only focus:absolute focus:p-4">Skip to content</a>
    <Nav />
    <main id="main" class="mx-auto w-full max-w-3xl px-6 pb-16 pt-10 md:pt-14"><slot /></main>
    <Footer />
  </body>
</html>
